{
  "name": "openapi-generator",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prettier-format": "prettier --config .prettierrc 'src/**/*.ts' --write",
    "validate": "openapi-generator-cli validate -i src/api.json",
    "generate": "openapi-generator-cli generate -i src/api.json -g typescript-node -o out -c config.json"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.5.1",
    "prettier": "^2.7.1"
  }
}
